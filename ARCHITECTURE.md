# 🏗️ Flipr Drive Architecture Overview

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                           CLIENT BROWSER                             │
│                      http://localhost:5173                           │
└──────────────────────────────┬───────────────────────────────────────┘
                               │
                               │ HTTP Requests
                               │
┌──────────────────────────────▼───────────────────────────────────────┐
│                         REACT FRONTEND                                │
│                                                                       │
│  ┌─────────────────┐  ┌──────────────────┐  ┌──────────────────┐  │
│  │   Public Pages  │  │   Admin Login    │  │ Admin Dashboard  │  │
│  │   - Home        │  │   - JWT Storage  │  │ - Protected      │  │
│  │   - Portfolio   │  │   - Auth Context │  │ - Secured Routes │  │
│  └─────────────────┘  └──────────────────┘  └──────────────────┘  │
│                                                                       │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                    API Service Layer                          │  │
│  │  - Centralized API calls                                      │  │
│  │  - JWT token management                                       │  │
│  │  - Request/Response handling                                  │  │
│  └───────────────────────────────────────────────────────────────┘  │
└──────────────────────────────┬───────────────────────────────────────┘
                               │
                               │ REST API Calls
                               │ Authorization: Bearer <JWT>
                               │
┌──────────────────────────────▼───────────────────────────────────────┐
│                      SPRING BOOT BACKEND                              │
│                      http://localhost:8080                            │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────────┐ │
│  │                    Security Layer                               │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐ │ │
│  │  │   CORS       │  │ JWT Filter   │  │  Spring Security    │ │ │
│  │  │  Configuration│ │ Validates    │  │  - Authentication   │ │ │
│  │  │              │  │  JWT Tokens  │  │  - Authorization    │ │ │
│  │  └──────────────┘  └──────────────┘  └──────────────────────┘ │ │
│  └─────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────────┐ │
│  │                    Controllers (REST API)                       │ │
│  │  ┌────────────┐ ┌──────────┐ ┌──────────┐ ┌────────────────┐  │ │
│  │  │    Auth    │ │ Projects │ │ Clients  │ │    Contact     │  │ │
│  │  │ Controller │ │Controller│ │Controller│ │   Controller   │  │ │
│  │  └────────────┘ └──────────┘ └──────────┘ └────────────────┘  │ │
│  │  - /api/auth/*      - /api/admin/projects    - /api/contact   │ │
│  │  - /api/public/*    - /api/admin/clients     - /api/newsletter│ │
│  └─────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────────┐ │
│  │                    Service Layer                                │ │
│  │  - Business Logic                                               │ │
│  │  - Data Validation                                              │ │
│  │  - Password Encryption (BCrypt)                                 │ │
│  └─────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────────┐ │
│  │                    Repository Layer                             │ │
│  │  - MongoDB Operations                                           │ │
│  │  - CRUD Methods                                                 │ │
│  │  - Query Methods                                                │ │
│  └─────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────┬───────────────────────────────────────┘
                               │
                               │ MongoDB Driver
                               │ Connection String
                               │
┌──────────────────────────────▼───────────────────────────────────────┐
│                        MONGODB ATLAS                                  │
│                         (Cloud Database)                              │
│                                                                       │
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────────────────┐  │
│  │   admins     │  │   projects   │  │   clients (leads)       │  │
│  └──────────────┘  └──────────────┘  └─────────────────────────┘  │
│                                                                       │
│  ┌──────────────────────┐  ┌─────────────────────────────────────┐  │
│  │ contactSubmissions   │  │      subscribers                   │  │
│  └──────────────────────┘  └─────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────────────┘
```

---

## Authentication Flow

```
┌─────────────┐                              ┌─────────────┐
│   Browser   │                              │   Backend   │
└──────┬──────┘                              └──────┬──────┘
       │                                            │
       │  1. POST /api/auth/login                  │
       │     { username, password }                │
       │────────────────────────────────────────>  │
       │                                            │
       │                                     2. Validate credentials
       │                                        & Generate JWT
       │                                            │
       │  3. Return JWT token                      │
       │  <────────────────────────────────────────│
       │     { token, username, role }             │
       │                                            │
  4. Store JWT                                     │
  in localStorage                                  │
       │                                            │
       │  5. API Request with JWT                  │
       │     Authorization: Bearer <token>         │
       │────────────────────────────────────────>  │
       │                                            │
       │                                     6. Validate JWT
       │                                        & Process request
       │                                            │
       │  7. Return Data                           │
       │  <────────────────────────────────────────│
       │                                            │
```

---

## Request/Response Flow Examples

### Public Request (No Auth)
```
GET /api/public/projects
───────────────────────────────────>
                                     [Controller]
                                         │
                                     [Service]
                                         │
                                    [Repository]
                                         │
                                    [MongoDB]
                                         │
<───────────────────────────────────
    200 OK
    [ { project1 }, { project2 } ]
```

### Admin Request (With Auth)
```
POST /api/admin/projects
Authorization: Bearer <JWT-TOKEN>
Body: { title, description, ... }
───────────────────────────────────>
                                     [JWT Filter]
                                     Validate Token ✓
                                         │
                                     [Controller]
                                     Check Authorization ✓
                                         │
                                     [Service]
                                     Business Logic
                                         │
                                    [Repository]
                                     Save to DB
                                         │
                                    [MongoDB]
                                         │
<───────────────────────────────────
    201 Created
    { id, title, description, ... }
```

---

## Data Models

### Admin Model
```javascript
{
  "_id": "ObjectId",
  "username": "admin",
  "password": "$2a$10$...", // BCrypt hash
  "email": "admin@flipr.com",
  "role": "ADMIN",
  "active": true,
  "createdAt": "2026-01-30T10:00:00",
  "lastLoginAt": "2026-01-30T15:30:00"
}
```

### Project Model
```javascript
{
  "_id": "ObjectId",
  "title": "E-Commerce Platform",
  "description": "...",
  "category": "Web Development",
  "technologies": ["React", "Node.js"],
  "imageUrl": "/uploads/project1.jpg",
  "featured": true,
  "completedDate": "2026-01-15T00:00:00",
  "clientName": "TechStore Inc",
  "projectUrl": "https://..."
}
```

### Client/Lead Model
```javascript
{
  "_id": "ObjectId",
  "name": "John Smith",
  "email": "john@example.com",
  "phone": "+1-555-0123",
  "company": "Tech Innovations Inc",
  "projectType": "Web Development",
  "budget": "10000-25000",
  "timeline": "2-3 months",
  "description": "...",
  "status": "NEW", // NEW, CONTACTED, QUALIFIED, CONVERTED
  "submittedAt": "2026-01-28T14:30:00",
  "notes": null
}
```

---

## Security Implementation

### 1. Password Security
```
User Password: "admin123"
       │
       ▼
   [BCrypt Hash]
       │
       ▼
Stored: "$2a$10$N9qo8uLOickgx2ZMRZoMye..."
```

### 2. JWT Token Structure
```
Header:
{
  "alg": "HS256",
  "typ": "JWT"
}

Payload:
{
  "sub": "admin",
  "role": "ADMIN",
  "iat": 1706620800,
  "exp": 1706707200
}

Signature:
HMACSHA256(
  base64UrlEncode(header) + "." +
  base64UrlEncode(payload),
  secret-key
)
```

### 3. Request Authorization
```
1. Client includes JWT in header
   ↓
2. JwtAuthenticationFilter intercepts request
   ↓
3. Extract and validate JWT
   ↓
4. Set SecurityContext with user details
   ↓
5. Controller checks if user has required role
   ↓
6. Process request if authorized
```

---

## Port Configuration

| Service          | Port | URL                          |
|-----------------|------|------------------------------|
| Frontend (Vite) | 5173 | http://localhost:5173        |
| Backend (Spring)| 8080 | http://localhost:8080/api    |
| MongoDB Atlas   | -    | Cloud (no local port)        |

---

## File Upload Flow

```
┌─────────────┐                              ┌─────────────┐
│   Browser   │                              │   Backend   │
└──────┬──────┘                              └──────┬──────┘
       │                                            │
       │  POST /api/admin/projects/{id}/image      │
       │  Content-Type: multipart/form-data        │
       │  Authorization: Bearer <token>            │
       │────────────────────────────────────────>  │
       │                                            │
       │                                     Validate JWT
       │                                     Save file to
       │                                     /uploads folder
       │                                     Update project
       │                                     imageUrl in DB
       │                                            │
       │  Return success                           │
       │  <────────────────────────────────────────│
       │  { imageUrl: "/uploads/file.jpg" }        │
       │                                            │
```

---

## Environment Configuration

### Development
```
Frontend: localhost:5173
Backend:  localhost:8080
Database: MongoDB Atlas (Cloud)
CORS:     Enabled for localhost
```

### Production
```
Frontend: your-domain.com
Backend:  api.your-domain.com
Database: MongoDB Atlas (Cloud)
CORS:     Enabled for your-domain.com only
HTTPS:    Required
```

---

## API Endpoint Summary

### Public Endpoints (🌐 No Auth)
- `POST /api/auth/login` - Admin login
- `GET /api/public/projects` - List projects
- `GET /api/public/projects/{id}` - Get project
- `POST /api/public/clients` - Submit lead
- `POST /api/contact` - Submit contact
- `POST /api/newsletter/subscribe` - Subscribe

### Admin Endpoints (🔒 Auth Required)
- `GET /api/auth/validate` - Validate token
- `POST /api/admin/projects` - Create project
- `PUT /api/admin/projects/{id}` - Update project
- `DELETE /api/admin/projects/{id}` - Delete project
- `POST /api/admin/projects/{id}/image` - Upload image
- `GET /api/admin/clients` - List leads
- `PATCH /api/admin/clients/{id}/status` - Update status
- `DELETE /api/admin/clients/{id}` - Delete lead
- `GET /api/admin/contacts` - List contacts
- `PATCH /api/admin/contacts/{id}/read` - Mark read
- `DELETE /api/admin/contacts/{id}` - Delete contact
- `GET /api/admin/subscribers` - List subscribers

---

This architecture ensures:
- ✅ Separation of concerns
- ✅ Secure authentication
- ✅ Scalable structure
- ✅ Easy maintenance
- ✅ Clear data flow
